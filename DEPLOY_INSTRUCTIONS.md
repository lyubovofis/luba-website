# Деплой на Netlify - Инструкция

## ✅ Что исправлено

### 1. База данных Supabase
- **Проблема:** RLS включен, но политика SELECT была только для authenticated
- **Решение:** Добавлена политика для anon роли:
```sql
CREATE POLICY "Allow anon to read quiz leads" 
ON quiz_leads 
FOR SELECT 
TO anon 
USING (true);
```

### 2. CRM Dashboard
- **Улучшена обработка ошибок** в функции loadLeads()
- Добавлены информативные сообщения об ошибках
- Логирование для отладки

### 3. Тестовая страница
- Создан файл `test-crm-connection.html` для проверки подключения
- Автоматически проверяет соединение с Supabase
- Показывает последние 10 лидов

## 🚀 Как задеплоить

### Вариант 1: Через Netlify CLI

```bash
cd "C:\Luba Website"
netlify deploy --prod
```

### Вариант 2: Через Git

```bash
cd "C:\Luba Website"
git add .
git commit -m "Fix: CRM Supabase connection with RLS policies"
git push origin main
```

Netlify автоматически задеплоит при push в main ветку.

### Вариант 3: Через Netlify UI
1. Зайти на https://app.netlify.com
2. Перетащить папку проекта в окно деплоя

## 🧪 Тестирование после деплоя

### 1. Проверка подключения CRM
